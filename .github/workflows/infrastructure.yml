name: Infrastructure

on:
  workflow_dispatch:

jobs:
  setup-infrastructure:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify Tailscale secrets are set
        env:
          TS_CLIENT_ID: ${{ secrets.TS_CLIENT_ID }}
          TS_CLIENT_SECRET: ${{ secrets.TS_CLIENT_SECRET }}
        run: |
          if [ -z "$TS_CLIENT_ID" ]; then
            echo "ERROR: TS_CLIENT_ID secret is empty or not accessible to this repository"
            exit 1
          fi
          if [ -z "$TS_CLIENT_SECRET" ]; then
            echo "ERROR: TS_CLIENT_SECRET secret is empty or not accessible to this repository"
            exit 1
          fi
          echo "Tailscale credentials are present"

      - name: Connect to Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_CLIENT_SECRET }}

      - name: Configure kubectl
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Apply namespace
        run: kubectl apply -f k8s/namespace.yaml

      - name: Apply ConfigMap
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GCP_LOCATION: ${{ secrets.GCP_LOCATION }}
          CLOUD_TASKS_QUEUE: ${{ secrets.CLOUD_TASKS_QUEUE }}
        run: envsubst < k8s/configmap.yaml | kubectl apply -f -

      - name: Apply backend service
        run: kubectl apply -f k8s/service-backend.yaml

      - name: Apply cloudtasks service
        run: kubectl apply -f k8s/service-cloudtasks.yaml
