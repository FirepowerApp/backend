apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production scaffolding. Creates the firepower namespace and applies the infra
# base unchanged (the base already carries production config values).
namespace: firepower

resources:
  - namespace.yaml
  - ../../../base/infra
